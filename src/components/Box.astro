---
interface Props {
	title?: string;
}
const { title = false } = Astro.props;
---
<div>
  <br class="mof--hidden">
  <div class="box">
    { title && (
      <h2 class="section">{title}</h2>
    )}
    <slot />
   </div>
</div>